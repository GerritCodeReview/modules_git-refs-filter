[access]
	inheritFrom = Public-Plugins
[submit]
	action = inherit
[access "refs/*"]
	owner = group modules-refsfilter
	push = +force group modules-refsfilter
[project]
	description = Gerrit lib module to allow filtering out refs in the Git advertizing protocol phase
